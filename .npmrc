# Faster installs with concurrent downloads
fetch-retries=3
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=60000

# Use lockfile
package-lock=true

# Save exact versions
save-exact=false
save-prefix=^

# Disable package-lock on publishing
package-lock-only=false

